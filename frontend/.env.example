# Leave empty for same-origin requests (default in production, routed by the k3s Ingress
# path rules for mtracker.nikichxp.xyz). Only set this for local dev against a remote backend.
VITE_API_BASE_URL=
